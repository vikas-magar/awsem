[package]
name = "awsem-events"
version = "0.1.0"
edition = "2024"

[lib]
name = "awsem_events"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["sync"] }
